dependencies:
  pre:
    - pip install flake8

test:
  override:
    - flake8:
        parallel: true
        files:
          - dynamodb-create-cloudwatch-alarms/**/*.py
    - dynamodb-create-cloudwatch-alarms --
